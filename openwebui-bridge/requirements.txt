# Agent Tool Bridge API Requirements
# FastAPI backend for Open WebUI integration with cybersecurity agents

# Core FastAPI framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# HTTP and networking
httpx>=0.25.2
requests>=2.31.0
aiohttp>=3.9.1

# Authentication and security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Database and data handling
sqlalchemy>=2.0.23
alembic>=1.13.1
redis>=5.0.1

# Async and concurrency
asyncio-mqtt>=0.13.0
asyncpg>=0.29.0

# Logging and monitoring
structlog>=23.2.0
prometheus-client>=0.19.0

# Configuration management
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Date and time handling
python-dateutil>=2.8.2
pytz>=2023.3

# Data validation and parsing
marshmallow>=3.20.1
cerberus>=1.3.5

# File handling and utilities
pathlib2>=2.3.7
click>=8.1.7

# Development and testing (optional)
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
httpx>=0.25.2  # For testing HTTP requests

# Documentation
mkdocs>=1.5.3
mkdocs-material>=9.4.8

# Type hints and code quality
mypy>=1.7.1
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0

# Windows debugging support (mcp-windbg integration)
# Note: These require Windows Debugging Tools to be installed
# asyncio (built-in)
# subprocess (built-in)

# Agent tool dependencies
# These are inherited from the existing tool ecosystem
# pydantic already included above
# requests already included above
# datetime (built-in)
# typing (built-in)
# logging (built-in)

# Optional: For enhanced error handling and monitoring
sentry-sdk[fastapi]>=1.38.0
rollbar>=0.16.3

# Optional: For rate limiting and API protection  
slowapi>=0.1.9
redis>=5.0.1  # Already included above

# Optional: For OpenAPI documentation enhancement
fastapi-utils>=0.2.1

# CORS support (included in FastAPI)
# python-cors already handled by FastAPI middleware

# Environment variable management
# python-dotenv already included above
