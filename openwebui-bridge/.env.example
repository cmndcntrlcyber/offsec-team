# Agent Tool Bridge Environment Configuration
# For Open WebUI Integration with chat.attck.nexus
# Copy this to .env and fill in your actual values

# Bridge Server Configuration
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8000
LOG_LEVEL=INFO

# Open WebUI Configuration
OPEN_WEBUI_ENDPOINT=https://your-domain.com/

# Authentication Tokens (REPLACE WITH YOUR ACTUAL TOKENS)
CHAT_NEXUS_BEARER=your_chat_nexus_bearer_token_here
AGENT_MCP_TOKEN=your_agent_mcp_token_here

# Windows Debugging Tools (Auto-detect)
CDB_PATH=
WINDBG_SYMBOLS_PATH=SRV*C:\Symbols*https://msdl.microsoft.com/download/symbols

# Cloudflare Zone Configuration (REPLACE WITH YOUR ZONES)
CF_ZONE_ATTCK_NEXUS=your-primary-domain.com
CF_ZONE_C3S_NEXUS=your-secondary-domain.com

# Security Configuration (REPLACE WITH YOUR ALLOWED ORIGINS)
CORS_ORIGINS=https://your-domain.com,http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080

# Development Settings (UPDATE TO YOUR PROJECT PATH)
PYTHONPATH=C:\path\to\your\project
